name: Selenium tests

on: push

jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker compose up -d
      - run: sleep 10
      - run: docker exec gradle_tests gradle test
      - if: always()
        run: docker compose down